# ===================================
# PAYMENT SERVICE - ENVIRONMENT VARIABLES
# ===================================
#
# This is a template for local development.
# For specific environments, see:
#   - .env.staging.example  (for Railway/staging deployment)
#   - .env.production.example (for production deployment)
#
# Copy this file to .env and fill in your values
# ===================================

# Server Configuration
PORT=8080              # gRPC server port
HTTP_PORT=8081         # HTTP server port for cron endpoints and Browser Post

# Database Configuration (Local Development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=payment_service
DB_SSL_MODE=disable    # Use 'require' for cloud databases
DB_MAX_CONNS=25
DB_MIN_CONNS=5

# EPX Gateway Configuration (SANDBOX for development)
# ⚠️  Get sandbox credentials from North/EPX support
EPX_BASE_URL=https://secure.epxuap.com
EPX_TIMEOUT=30

# EPX Credentials (Sandbox)
EPX_CUST_NBR=9001           # Replace with your sandbox customer number
EPX_MERCH_NBR=900300        # Replace with your sandbox merchant number
EPX_DBA_NBR=2               # Replace with your sandbox DBA number
EPX_TERMINAL_NBR=77         # Replace with your sandbox terminal number

# North Merchant Reporting API (for chargeback/dispute data)
# Note: Even sandbox uses the production API URL
NORTH_API_URL=https://api.north.com
NORTH_TIMEOUT=30

# Browser Post Configuration
# For local development, use localhost
CALLBACK_BASE_URL=http://localhost:8081

# Cron Authentication
# Secret token for authenticating cron job HTTP requests
CRON_SECRET=dev-secret-change-in-production

# Environment
ENVIRONMENT=development

# Logging
LOG_LEVEL=debug         # debug, info, warn, error

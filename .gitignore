# Binaries
bin/
/server
/admin
/seed
/paycli
/payment-service
!cmd/server/*.go
!cmd/admin/*.go
!cmd/seed/*.go
!cmd/paycli/*.go
*.test

# Environment
.env

# VSCode
.vscode

# Claude Code
.claude
# Never commit secrets directory
secrets/

# Test coverage files
coverage.out
*.coverprofile

# ================================================
# SECRETS AND CREDENTIALS (NEVER COMMIT!)
# ================================================

# Oracle Cloud credentials
*.pem
*.key
*.oci/
.oci/
oci_*.pem
oci_*.pub
oracle-*.pem
oracle-wallet/
oracle-wallet.zip
oracle-config.env
oracle-setup-secrets.txt

# Environment variables with secrets
.env
.env.*
!.env.example
!.env.*.example
*.env.local
*.env.staging
*.env.production

# GitHub secrets backup
github-secrets*.txt
SETUP_CREDENTIALS.txt

# Database credentials
db-credentials.txt
*-credentials.txt
*_credentials.txt
*_credentials.json
service_*_credentials.json
seed_credentials.txt

# SSH keys
id_rsa
id_rsa.pub
*-key
*-key.pub
*.ppk

# Terraform sensitive files
terraform.tfvars
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Temporary secrets
secrets/
tmp-secrets/
*.secret

# Auth tokens
*token*.txt
*auth*.txt
keys/
*.backup

# Test credentials (NEVER COMMIT!)
# These are generated dynamically during test execution
tests/fixtures/auth/test_services.json

# Test artifacts and temporary files
test-*.json
grant-*.json
update-*.json
merchant_*.json
apartment-*.json
*_info.json

# Old reports
COMPREHENSIVE_TEST_REPORT.md
